{
  "name": "@probex/basic",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx --watch src/index.ts",
    "build": "rm -rf ./dist && tsc --noEmit && esbuild src/index.ts --outdir=dist --platform=node --format=esm --sourcemap --target=node22 --bundle"
  },
  "keywords": [],
  "author": "Artem Horovyi",
  "license": "MIT",
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fastify": "^5.11.2"
  }
}
